<nav class="navbar is-primary" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to("Friend Feed", root_path, class: "navbar-item") %>
      <% if user_signed_in? %>
        <%= link_to(current_user.name, user_path(current_user), class: "navbar-item") %>
        <!-- temp link to root path until search is built out -->
        <%= link_to("Search", root_path, class: "navbar-item") %>
        <%= link_to("Notifications", notifications_path, class: "navbar-item") %> 
      <% end %>
    </div>

    <div class="navbar-end">
        <div class="buttons">
          <% if user_signed_in? %>
            <%= link_to("Sign Out", destroy_user_session_path, method: :delete, class: "button is-light") %>
          <% else %>
            <%= link_to("Sign In", new_user_session_path, class: "button is-primary is-outlined is-small is-inverted") %>
            <%= link_to("Sign Up", new_user_registration_path, class: "button is-primary is-outlined is-small is-inverted") %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
